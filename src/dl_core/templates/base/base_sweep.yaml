# Base Sweep Template

template_name: my_experiment_sweep
description: "Generic sweep template"
version: "1.0"
base_config: "./base.yaml"

fixed:
  runtime:
    log_level: INFO
  trainer:
    standard:
      name: standard
      epochs: 1

default_grid: {}

tracking:
  # experiment_name: my_project
  # Optional tracker destination override. Defaults to experiment.name or the
  # repository root name.
  # sweep_name: my_custom_sweep
  # Optional sweep grouping override. Defaults to the sweep filename.
  run_name_template: "lr_{optimizers.lr}"
  description_template: "learning_rate={optimizers.lr}"
  auto_tags:
    optimizer: "from:optimizers.name"
    learning_rate: "from:optimizers.lr"

seeds: [2025]

preset_configs: {}
